# Скопируйте в .env в корне проекта (рядом с docker-compose.yml)
# cp .env.example .env

POSTGRES_USER=educa
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=educa

REACT_APP_BACKEND_URL=https://ct-kubik.by
